{
  "name": "concept.io",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "client/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "dev:all": "concurrently --names \"CLIENT,SERVER,DIFFUSION\" -c \"cyan,green,magenta\" \"npm run dev:client\" \"npm run dev:server\" \"npm run dev:diffusion\"",
    "dev:client": "cd client/concept.io && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev:diffusion": "cd server/diffusion-service && conda run -n project uvicorn server:app --host 0.0.0.0 --port 8000 --reload",
    "setup:diffusion": "cd server/diffusion-service && conda run -n project pip install -r requirements.txt",
    "build": "concurrently \"npm run build:client\" \"npm run build:server\"",
    "build:client": "cd client/concept.io && npm run build",
    "build:server": "cd server && npm run build",
    "install:all": "npm install && npm run install:client && npm run install:server && npm run setup:diffusion",
    "install:client": "cd client/concept.io && npm install",
    "install:server": "cd server && npm install",
    "clean": "npm run clean:client && npm run clean:server",
    "clean:client": "cd client/concept.io && npm run clean",
    "clean:server": "cd server && npm run clean",
    "start:windows": "powershell -ExecutionPolicy Bypass -File start-dev.ps1"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "concurrently": "^8.2.1"
  },
  "devDependencies": {
    "prettier": "3.6.2"
  }
}
